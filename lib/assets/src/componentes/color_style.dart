import 'dart:html';

import 'package:flutter/material.dart';

class ColorStyle{
  static const primary =  const Color.fromRGBO(0, 0, 0, 1);
  static const secondary =  const Color.fromRGBO(0, 0, 0, 1);
  static const Background = Color.fromARGB(240, 0, 0, 0);

}